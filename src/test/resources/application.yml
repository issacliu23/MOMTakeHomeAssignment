spring:
  datasource:
    url: jdbc:h2:mem:momdb
    username: sa
    password: password
  jpa:
    hibernate.ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect
      hibernate.format_sql: true